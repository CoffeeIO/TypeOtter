'use strict';

describe('Main simple test', function () {

    it('should be equal to 3', function () {
        expect(3).toEqual(3);
    });

    // Multiple tests
    it('should be equal to 3', function () {
        expect(4).toEqual(4);
    });

});
